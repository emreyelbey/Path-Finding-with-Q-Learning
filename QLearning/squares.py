class Squares:
    def __init__(self, x_pos, y_pos, color_r, color_g, color_b, is_obs):
        self.x_pos = x_pos
        self.y_pos = y_pos
        self.color_r = color_r
        self.color_g = color_g
        self.color_b = color_b
        self.is_obs = is_obs